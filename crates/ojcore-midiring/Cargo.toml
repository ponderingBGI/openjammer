[package]
name = "ojcore-midiring"
description = "Lock-free SPSC byte ring with a frozen #[repr(C)] layout for the JS<->wasm SharedArrayBuffer boundary."
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
