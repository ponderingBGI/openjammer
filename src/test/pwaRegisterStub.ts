/** Test stub for the vite-plugin-pwa `virtual:pwa-register` module (no SW in jsdom). */
export function registerSW(_options?: unknown): (reloadPage?: boolean) => Promise<void> {
    return async () => {};
}
