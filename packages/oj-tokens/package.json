{
  "name": "@openjammer/oj-tokens",
  "version": "0.0.0",
  "description": "OpenJammer design tokens (DTCG) compiled by Style Dictionary — the single source of truth for the theme system (primitive + semantic + per-theme), consumed by the app, oj-ui, Figma, and the AI design agent.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./variables.css": "./dist/variables.css"
  },
  "files": [
    "src",
    "dist",
    "tokens",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
