{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set the OpenJammer web UI needs: invoke core commands plus the opener plugin for external links.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
