# Cargo build output (workspace target is at the repo root; this is a safety net).
/target

# Auto-generated by `tauri-build` at compile time (capability JSON schemas).
# Regenerated on every build; never committed.
/gen/schemas
