<!-- Thanks for contributing to OpenJammer! Target `canari`, not `main`. -->

## What & why

<!-- What does this change, and which belief/value does it honor (perception you can feel,
     or a minimal core kept small so the community can make it their own)? -->

## Checklist

- [ ] Targeted **`canari`** (not `main`)
- [ ] Used **`bun`** for all package operations
- [ ] **Tested with audio actually playing** — no clicks, dropouts, or added latency
- [ ] Nodes connect/disconnect cleanly; undo/redo works
- [ ] No console errors or warnings

## Sign-off (required)

By opening this PR I agree to the contributor terms in [CONTRIBUTING.md](../CONTRIBUTING.md):
my contributions are licensed under **AGPL-3.0-only WITH the OpenJammer Plugin Exception**, and the
steward may apply future versions of that Exception.

- [ ] **Every commit is signed off** (`git commit -s` → `Signed-off-by:`), certifying the
      [DCO 1.1](../.github/DCO). The DCO check will fail otherwise — fix with `git commit --amend -s`
      or `git rebase --signoff`.
