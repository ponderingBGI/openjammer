{
  "name": "@openjammer/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "OpenJammer documentation site (Astro + Starlight). Workspace-ISOLATED: this package has its own lockfile and is NOT a member of the root bun workspace (root workspaces = ['packages/*']), to firewall the Astro/Zod dependency tree from the app's. Install + build from inside apps/docs.",
  "scripts": {
    "gen:components": "bun scripts/gen-component-index.mjs",
    "dev": "bun scripts/gen-component-index.mjs && astro dev",
    "build": "bun scripts/gen-component-index.mjs && astro build",
    "preview": "astro preview",
    "check": "bun scripts/gen-component-index.mjs && astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.34.3",
    "astro": "^5.6.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "starlight-typedoc": "0.21.0",
    "typedoc": "^0.28.19",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^6.0.3"
  }
}
