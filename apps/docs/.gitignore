# Astro build output + generated type cache
dist/
.astro/

# starlight-typedoc output — generated from oj-protocol-ts on every build.
src/content/docs/api/

# "Find the code" index — generated from the repo-root component-map.json on every
# docs build (apps/docs/scripts/gen-component-index.mjs).
src/content/docs/design-system/components.mdx

# This package is workspace-ISOLATED: it has its own bun.lock (committed), and
# its own node_modules (ignored). Installed/built from inside apps/docs.
node_modules/
