<!DOCTYPE html>
<!-- Generated by `just licenses` (cargo-about + about.toml). Do not edit by hand.
     Bundle this into the installer payload alongside LICENSE + LICENSE-EXCEPTION.md.
     This is Part A (Rust deps) of THIRD-PARTY-LICENSES.md; Part B is hand-maintained. -->
<html>
  <head>
    <meta charset="utf-8" />
    <title>OpenJammer — Third-Party Rust Licenses</title>
    <style>
      body { font-family: system-ui, sans-serif; max-width: 60rem; margin: 2rem auto; padding: 0 1rem; }
      h2 { margin-top: 2rem; border-top: 1px solid #ccc; padding-top: 1rem; }
      pre { white-space: pre-wrap; background: #f6f6f6; padding: 1rem; font-size: 0.85rem; }
      .crates { color: #555; }
    </style>
  </head>
  <body>
    <h1>Third-Party Rust Licenses</h1>
    <p>Licenses of the Rust dependencies bundled with OpenJammer. OpenJammer itself is
       AGPL-3.0-only WITH the OpenJammer Plugin Exception (see LICENSE + LICENSE-EXCEPTION.md).</p>
    {{#each licenses}}
    <h2>{{name}}</h2>
    <p class="crates">Used by:
      {{#each used_by}}{{crate.name}} {{crate.version}}{{#unless @last}}, {{/unless}}{{/each}}
    </p>
    <pre>{{text}}</pre>
    {{/each}}
  </body>
</html>
